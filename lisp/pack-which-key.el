;;; pack-which-key.el --- Keymap navigation -*- lexical-binding: t -*-
;;; Commentary:
;;; Code:

(use-package which-key
  :diminish which-key-mode
  :config
  (which-key-mode 1))

(provide 'pack-which-key)
;;; pack-which-key.el ends here
